<% if @user.errors.any? %>
  <h2><%= pluralize(@user.errors.count, 'error') %> prohibited this book from being saved:</h2>
  <ul>
    <% @user.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>


<h1 style="text-align: center;">Users info</h1>

<div style="display: flex; justify-content: center; align-items: center;">
  <%= form_with model: @user do |f| %>
    <div style="text-align: left;">
      <label for="inputName">Name</label><br>
      <%= f.text_field :name, autofocus: true, id: "inputName", placeholder: "名前" %><br><br>

      <label for="inputImage">Image</label><br>
      <%= f.file_field :profile_image, placeholder: "プロフィール画像", accept: "image/*" %><br><br>
      
      <label for="inputbody">introduction</label><br>
      <%= f.text_area :introduction, autofocus: true, id: "inputbody", placeholder: "introduction" %><br><br>

      <%= f.submit "Update User", class: "btn btn-secondary" %>
    </div>
  <% end %>
</div>
